# Database — SQLite (local file-based)
# File is created in prisma/dev.db on first `bun run db:push`
# This works in local dev and in the preview sandbox.
# For Vercel deployment, see note below.
DATABASE_URL="file:./dev.db"
